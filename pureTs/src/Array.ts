const arr: string[] = [];
const arr1: Array<string> = [];

type User = {
  name: string;
  isActive: boolean;
};

const arr2: User[] = [];
const arr3: Array<User> = [];
arr3.push({ name: "shahariaz", isActive: true });

//array of arrays

const MlModal: number[][] = [];
MlModal.push([1, 2, 3]);

export {};
